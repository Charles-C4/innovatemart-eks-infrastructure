name: "Project Bedrock CI/CD"

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  terraform:
    name: "Terraform"
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: "us-east-1"

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_wrapper: false # Important for some EKS authentication workflows

      - name: Terraform Init
        run: terraform init

      - name: Update Kubeconfig
        run: aws eks update-kubeconfig --region us-east-1 --name project-bedrock-cluster

      - name: Terraform Plan
        if: github.event_name == 'pull_request'
        run: terraform plan -no-color

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve

      - name: Generate Grading Data
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
        
          # Note: If you want this committed back to the repo automatically, 
          # you would add git commands here. Otherwise, run it locally once 
          # and push it to the repo manually.